# Core scientific computing (Python 3.7 compatible versions)
pandas>=1.1.0,<1.4.0
numpy>=1.16.0,<1.22.0
tqdm>=4.50.0,<5.0.0

# Jupyter ecosystem
jupyter>=1.0.0
ipykernel>=5.3.0
ipywidgets>=7.5.0,<8.0.0

# Excel export support
openpyxl>=3.0.0,<4.0.0
xlsxwriter>=1.3.0,<4.0.0

# Standard library dependencies (usually included but explicit for clarity)
itertools-recipes>=0.0.2  # If needed for extended itertools functionality

# DFG Rating package from GitHub repository
git+https://github.com/spoho-datascience/dfg-rating.git@feature/replicability-seeding